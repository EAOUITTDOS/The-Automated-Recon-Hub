import subprocess

# --- Outlaw Ninja: Sniper Hub Centralized Manager ---
# Purpose: Orchestrate vulnerability scanning and secrets hunting

def run_scanner(tool_cmd):
    print(f"[*] Launching: {tool_cmd}")
    subprocess.run(tool_cmd, shell=True)

def menu():
    print("--- Sniper Hub v1.0 ---")
    print("1. Run Web Vulnerability Scan")
    print("2. Search for Leaked Secrets/API Keys")
    print("3. Execute Custom Hydra Multi-Head Logic")
    print("4. Exit")
    
    choice = input("\n[?] Select Operation: ")
    
    if choice == '1':
        target = input("Enter Target URL: ")
        run_scanner(f"nmap --script http-vuln* {target}")
    elif choice == '2':
        path = input("Enter Project Directory: ")
        run_scanner(f"grep -rE 'api_key|secret|password' {path}")
    elif choice == '3':
        print("[!] Activating Multi-Head Logic...")
        # Placeholder for our specific Hydra pipe logic
    elif choice == '4':
        exit()

if __name__ == "__main__":
    while True:
        menu()
